---
title: "I Built a Full-Stack Invoice App from Scratch. Here's the Complete Breakdown"
originalUrl: "https://dev.to/carter254g/i-built-a-full-stack-invoice-app-from-scratch-heres-the-complete-breakdown-2mmb"
date: "2026-05-01T22:41:01.015Z"
---

# I Built a Full-Stack Invoice App from Scratch. Here's the Complete Breakdown
# 我从零构建了一个全栈发票应用：完整技术拆解

Most invoice tools are either too expensive or too complicated. I built my own in one week and deployed it live. Here is every technical decision I made and what I learned. Live demo: https://invoxa-eta.vercel.app GitHub: https://github.com/Carter254g/invoxa

市面上的发票工具要么太贵，要么太复杂。我用一周时间构建了自己的应用并成功上线。以下是我做出的每一项技术决策以及我的心得体会。在线演示：https://invoxa-eta.vercel.app GitHub：https://github.com/Carter254g/invoxa

### What It Does
### 功能概览

Invoxa lets you create clients, generate invoices with line items, track payment status, and see your revenue on a dashboard. Multi-currency support included.

Invoxa 允许你创建客户、生成包含明细项的发票、跟踪付款状态，并在仪表板上查看收入情况。同时支持多币种。

### The Stack
### 技术栈

React on the frontend. Node.js and Express on the backend. PostgreSQL for the database. Deployed on Vercel and Render. Simple. No unnecessary complexity.

前端使用 React，后端使用 Node.js 和 Express，数据库采用 PostgreSQL。部署在 Vercel 和 Render 上。简单直接，没有不必要的复杂性。

### The Part Most Tutorials Skip — Auth Middleware
### 大多数教程忽略的部分——身份验证中间件

Every protected route in the API runs through this middleware before the controller even sees the request:

API 中的每个受保护路由在控制器接收请求之前，都会先经过此中间件：

```javascript
const auth = (req, res, next) => {
  const authHeader = req.headers.authorization;
  if (!authHeader || !authHeader.startsWith('Bearer ')) {
    return res.status(401).json({ error: 'No token provided' });
  }
  const token = authHeader.split(' ')[1];
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded;
    next();
  } catch (err) {
    return res.status(401).json({ error: 'Invalid or expired token' });
  }
};
```

Clean. Reusable. One function protects every route.

简洁且可复用。一个函数即可保护所有路由。

### Database Migrations on Startup
### 启动时的数据库迁移

Instead of manually creating tables, the server runs migrations automatically on startup:

服务器不再手动创建表，而是在启动时自动运行迁移：

```javascript
migrate.createTables().then(() => {
  app.listen(PORT, () => {
    console.log(`Server running on port ${PORT}`);
  });
});
```

Anyone who clones the repo gets a working database in seconds.

任何克隆该仓库的人都能在几秒钟内获得一个可用的数据库。

### The Axios Interceptor That Saved Me Hours
### 为我节省数小时的 Axios 拦截器

Instead of attaching the JWT token to every API call manually, one interceptor handles it globally:

无需手动将 JWT 令牌附加到每个 API 调用中，一个拦截器即可全局处理：

```javascript
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

Write it once. Forget about it.

一次编写，无需再操心。

### What Broke in Production
### 生产环境中的“坑”

Three things hit me during deployment:
1. Render requires SSL for PostgreSQL connections. Add `rejectUnauthorized: false` to your pg Pool config in production or nothing works.
2. CORS needs to explicitly list your Vercel domain. A wildcard does not work with credentials.
3. JWT expiry values from environment variables need to be strings. When the value comes back undefined from the env, `jwt.sign` throws a silent error. Hardcode the fallback.

部署过程中我遇到了三个问题：
1. Render 要求 PostgreSQL 连接必须使用 SSL。在生产环境的 pg Pool 配置中添加 `rejectUnauthorized: false`，否则无法连接。
2. CORS 需要明确列出你的 Vercel 域名。通配符在处理凭据时无效。
3. 环境变量中的 JWT 过期时间值必须是字符串。当从环境变量获取的值为 `undefined` 时，`jwt.sign` 会抛出一个静默错误。记得硬编码一个回退值。

These cost me two hours. Now they cost you nothing.

这些问题花了我两个小时，现在你不需要再浪费时间了。

### The Dashboard Pulls Real Data
### 仪表板展示真实数据

The dashboard shows total invoices, total clients, revenue collected, and outstanding balance — all calculated from live database queries on every load. No fake data. No hardcoded numbers.

仪表板显示发票总数、客户总数、已收收入和未结余额——所有数据均在每次加载时通过实时数据库查询计算得出。没有虚假数据，也没有硬编码的数字。

### What is Next
### 未来计划

* PDF export for invoices
* Email delivery via Nodemailer
* Recurring invoices
* Payment gateway integration

* 发票 PDF 导出
* 通过 Nodemailer 发送邮件
* 周期性发票
* 支付网关集成

### Try It and Star the Repo
### 试用并给仓库点个星

Live app: https://invoxa-eta.vercel.app
If this helped you, star the repo on GitHub and drop a comment below.
GitHub: https://github.com/Carter254g/invoxa

在线应用：https://invoxa-eta.vercel.app
如果这对你有帮助，请在 GitHub 上给仓库点个星，并在下方留下评论。
GitHub：https://github.com/Carter254g/invoxa