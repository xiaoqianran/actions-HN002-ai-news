---
title: "How to Make Claude Code Validate its own Work"
originalUrl: "https://towardsdatascience.com/how-to-make-claude-code-validate-its-own-work/"
date: "2026-05-05T22:27:09.177Z"
---

# How to Make Claude Code Validate its own Work
# 如何让 Claude Code 验证其自身的工作成果

In this article, I’ll cover how you can make Claude Code verify its own work to vastly improve Claude Code performance.
在本文中，我将介绍如何让 Claude Code 验证其自身的工作成果，从而大幅提升其性能。

Claude Code is a very powerful model out of the box. To leverage its full capabilities, however, you need to give it access to validate and verify its own work. In a previous article, I mentioned Claude validating its own work as an important part of how I optimize my own use of Claude Code. In this article, however, I’ll dive deeper into how I make Claude validate its own work.
Claude Code 开箱即用，功能非常强大。然而，要充分发挥其潜力，你需要让它能够验证和核实自己的工作。在之前的一篇文章中，我曾提到让 Claude 自行验证工作是我优化 Claude Code 使用体验的重要一环。在本文中，我将深入探讨我是如何让 Claude 实现这一点的。

The benefits are incredible. When you make Claude validate its own work, you get:
其益处令人惊叹。当你让 Claude 验证其自身工作时，你将获得：

* A model better at one-shotting implementations (spends less time iterating)
* 一个更擅长“一次性”完成实现（减少迭代时间）的模型
* A model that can run for longer (the model keeps going until it’s successfully able to verify its own work)
* 一个可以运行更长时间的模型（模型会持续工作，直到成功验证其自身成果）
* The model can complete more complex work
* 模型能够完成更复杂的任务

I’ll dive deeper into some specific tasks where I ask Claude to verify its own work, where I save a lot of time. I’ll also cover my thought process when setting up Claude in this way.
我将深入探讨一些我要求 Claude 验证其自身工作的具体任务，这些任务为我节省了大量时间。我还会分享我在以这种方式配置 Claude 时的思考过程。

### Why should you have Claude verify its own work?
### 为什么要让 Claude 验证其自身的工作？

The number one reason you should make Claude verify its own work is that it simply makes Claude perform better. You can imagine this with the following scenario: Imagine you had to implement a piece of code to calculate the Fibonacci sequence. Obviously, some people have done this exact task before, and it’s going to be relatively simple for them to do. However, imagine that you have to complete this task perfectly without ever getting the opportunity to run the code and see the output, i.e., you have to create the perfect code on your first attempt at the problem.
你应该让 Claude 验证其自身工作的首要原因是，这能直接提升 Claude 的表现。你可以通过以下场景来想象：假设你需要编写一段代码来计算斐波那契数列。显然，有些人以前做过这项任务，对他们来说这相对简单。但试想一下，如果你必须在完全没有机会运行代码、查看输出的情况下完美完成这项任务，也就是说，你必须在第一次尝试时就写出完美的代码。

So, naturally, this is way harder than if you get the opportunity to test the code yourself, tweak it if you see it’s not producing the exact correct numbers, and continue like that until your piece of code is producing the correct output. The same exact concept applies to Claude Code. If you don’t give it the chance to verify its own work, it’s like asking it to write code for the Fibonacci sequence without letting it ever see the output of the code. Obviously, you’re putting Claude Code in a worse position where it’s going to produce inferior results compared to when Claude Code gets the opportunity to test its own code.
显然，这比你有机会亲自测试代码、在发现输出结果不准确时进行微调，并不断重复直到代码产生正确输出要困难得多。同样的逻辑也适用于 Claude Code。如果你不给它验证自身工作的机会，就如同要求它编写斐波那契数列代码，却不让它看到代码的输出结果。显然，你让 Claude Code 处于不利地位，与它有机会测试自身代码的情况相比，它产生的结果会更差。

### How to make Claude verify work in practice
### 在实践中如何让 Claude 验证工作

The wording “make Claude verify its own work”, often gets thrown around, for example on LinkedIn and X. However, I notice relatively few people explaining exactly how they do it themselves, which makes it hard for others to replicate. Thus, I’ll cover some real-world examples of how I made Claude verify its own work.
“让 Claude 验证其自身工作”这种说法在 LinkedIn 和 X 等平台上经常被提及。然而，我注意到很少有人解释他们具体是如何操作的，这使得其他人难以效仿。因此，我将通过一些实际案例来介绍我是如何让 Claude 验证其自身工作的。

I’ll cover the process from:
我将涵盖以下流程：
* Hearing about a problem
* 了解问题
* Understanding what’s causing the problem
* 理解问题成因
* Implementing a solution with Claude and ensuring it can verify its own work
* 与 Claude 一起实施解决方案，并确保它能验证其自身工作

#### Long LLM processing times
#### LLM 处理时间过长

My first concrete example is a case where I was analyzing user data from an interaction with a conversational AI agent. After the conversation, I have to process the chat, such as fetching the transcript and performing classification and data extraction on the transcript. I started investigating the problem by reproducing it and running the LLM processing on the same conversation multiple times, and seeing how long it took. It turned out that the median and average time were relatively acceptable, around 30 seconds, but around every tenth time, processing time would be over two minutes, which is, of course, completely unacceptable.
我的第一个具体案例是分析与对话式 AI 代理交互的用户数据。对话结束后，我需要处理聊天记录，例如获取转录文本并对其进行分类和数据提取。我通过复现问题并多次对同一对话运行 LLM 处理来调查该问题，并观察其耗时。结果发现，中位数和平均时间相对可以接受，约为 30 秒，但大约每十次中就有一次处理时间超过两分钟，这显然是完全不可接受的。

I explained the situation to Claude Code and asked him what could be causing this issue. The most likely cause, it turned out, was that I was simply inputting a lot of tokens and outputting a lot of tokens, which in some situations take a lot of time to produce. Thus, the solution was to take this one single LLM call and split it into three to make the number of output tokens it had to produce fewer, so that it can run in parallel.
我向 Claude Code 解释了情况，并询问可能的原因。结果发现，最可能的原因是我输入和输出的 Token 数量过多，在某些情况下生成这些内容需要很长时间。因此，解决方案是将这一个 LLM 调用拆分为三个，以减少每个调用需要生成的输出 Token 数量，从而实现并行运行。

This is an example of a perfect task where Claude Code can verify its own work: A perfect task to verify your own work is a task where you have a known expected output you want to produce and you can keep working and iterating on the problem until you reach that exact output.
这是一个 Claude Code 可以验证其自身工作的完美任务示例：验证自身工作的完美任务是指，你有一个已知的预期输出，并且可以不断工作和迭代，直到达到该确切输出的任务。

This is great because what I have now is a number of input tokens that are run, and an expected output, which is what I expect if I do everything in one LLM call. And I can simply ask Claude Code to split a LLM call into three pieces and to make sure that you’ve done it correctly, compare the result from the split LLM calls versus the single monolithic LLM call, they are almost exactly the same (not exactly the same because LLMs are stochastic).
这非常棒，因为我现在拥有运行的输入 Token 数量以及预期的输出（即我如果在一个 LLM 调用中完成所有操作所期望的结果）。我可以简单地要求 Claude Code 将一个 LLM 调用拆分为三个部分，并为了确保它做得正确，将拆分后的 LLM 调用结果与单个整体 LLM 调用结果进行比较，它们几乎完全相同（由于 LLM 具有随机性，不会完全一致）。

I prompted my Claude Code instance with all this information. It kept iterating on its code until it ensured the outputs were the same, and it successfully one-shot the problem, coming back to me with a successful solution.
我将所有这些信息输入到我的 Claude Code 实例中。它不断迭代代码，直到确保输出结果一致，并成功地一次性解决了这个问题，最终为我提供了一个成功的解决方案。

#### Designing a web page
#### 设计网页

The last example I provided was great because it’s very simple for the LLM or Claude Code to verify the results. It can simply perform an API call, compare outputs, and see if it’s correct. However, what happens when the output you want to produce is a visual? My second example includes a problem where I received a design for what a web page should look like, and I wanted Claude Code to produce that exact design. Of course, given the framework of the application and the existing codebase it was written for. This might sound like a harder task because it involves visually looking at results. Luckily, we have Claude in Chrome, which is an MCP where you can give Claude access to your Google Chrome and let it visually inspect results. So I was provided with a screenshot of a design of what the page should look like, including how the...
我提供的上一个例子很棒，因为对于 LLM 或 Claude Code 来说，验证结果非常简单。它只需执行 API 调用、比较输出并查看是否正确即可。但是，当你想要生成的输出是视觉内容时会怎样呢？我的第二个例子涉及一个问题：我收到了一份网页设计稿，想要 Claude Code 生成完全一致的设计。当然，这是基于应用程序的框架和现有的代码库。这听起来可能是一项更艰巨的任务，因为它涉及视觉上的结果检查。幸运的是，我们有 Claude in Chrome，这是一个 MCP（模型上下文协议），你可以让 Claude 访问你的 Google Chrome 浏览器，并让它直观地检查结果。因此，我提供了一张页面设计截图，包括页面应该是什么样子，以及……