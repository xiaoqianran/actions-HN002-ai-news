---
title: "QASM-Eval: A Dataset to Train and Evaluate LLMs on OpenQASM-3 Beyond Quantum Circuits"
originalUrl: "https://arxiv.org/abs/2605.30358"
date: "2026-06-01T23:21:38.904Z"
---

# QASM-Eval: A Dataset to Train and Evaluate LLMs on OpenQASM-3 Beyond Quantum Circuits
# QASM-Eval：一个用于训练和评估大语言模型在 OpenQASM-3（超越量子电路）领域能力的基准数据集

**Abstract:** Quantum computing remains in the Noisy Intermediate-Scale Quantum (NISQ) era, where the performance is highly constrained to noise. Addressing the limitation often requires hardware-facing capabilities beyond gate-sequence circuit specification, including mid-circuit measurement and classical feedback for quantum error correction (QEC), precise timing control for dynamical decoupling (DD), and pulse-level waveform access for calibration.

**摘要：** 量子计算目前仍处于含噪声中等规模量子（NISQ）时代，其性能受到噪声的极大限制。要解决这一局限性，通常需要超越门序列电路规范的硬件级能力，包括用于量子纠错（QEC）的电路中测量与经典反馈、用于动力学解耦（DD）的精确时序控制，以及用于校准的脉冲级波形访问。

OpenQASM-3 was introduced to expose exactly these capabilities, providing a hardware-level programming interface. However, despite the rapid progress of large language models in code generation, there is still no dataset specifically designed to train and evaluate LLMs on OpenQASM-3 programs that involve its advanced hardware-oriented features.

OpenQASM-3 的推出正是为了提供这些能力，从而构建了一个硬件级的编程接口。然而，尽管大语言模型（LLM）在代码生成方面取得了飞速进展，但目前仍缺乏专门用于训练和评估 LLM 处理涉及高级硬件特性之 OpenQASM-3 程序的数据集。

To address this gap, we introduce QASM-Eval, the first comprehensive dataset designed to train and evaluate LLMs on OpenQASM-3. Rather than focusing on quantum algorithm design or reasoning, QASM-Eval explicitly targets the language's hardware-facing features. QASM-Eval comprises an expert-verified test set of 100 tasks and a training set of 4,000 tasks, systematically covering classical logic, timing scheduling, pulse control, and complex real-world workflows.

为了填补这一空白，我们推出了 QASM-Eval，这是首个旨在训练和评估 LLM 在 OpenQASM-3 上表现的综合数据集。QASM-Eval 不侧重于量子算法设计或推理，而是明确针对该语言的硬件特性。QASM-Eval 包含一个由专家验证的 100 个任务组成的测试集，以及一个包含 4,000 个任务的训练集，系统地涵盖了经典逻辑、时序调度、脉冲控制以及复杂的现实工作流。

To automatically validate generated programs, we check syntax, quantum states and program timeline using an extended verifier. Our evaluation reveals that while state-of-the-art LLMs struggle heavily in OpenQASM-3 coding tasks, targeted fine-tuning on QASM-Eval yields significant gains. QASM-Eval provides a crucial benchmark and training foundation to accelerate the development of reliable LLM assistants for hardware-facing quantum programming in NISQ era.

为了自动验证生成的程序，我们使用扩展验证器检查语法、量子态和程序时间线。我们的评估表明，尽管目前最先进的 LLM 在 OpenQASM-3 编码任务中表现吃力，但在 QASM-Eval 上进行针对性微调可带来显著提升。QASM-Eval 为加速开发 NISQ 时代面向硬件的量子编程可靠 LLM 助手提供了关键的基准和训练基础。