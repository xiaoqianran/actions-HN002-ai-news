---
title: "Clustering Guided Domain-Specific Pretrained Foundation Model Very High-Resolution Arctic Remote Sensing"
originalUrl: "https://arxiv.org/abs/2605.30467"
date: "2026-06-01T23:22:16.066Z"
---

# Clustering Guided Domain-Specific Pretrained Foundation Model for Very High-Resolution Arctic Remote Sensing
# 基于聚类引导的北极超高分辨率遥感领域专用预训练基础模型

**Abstract:** This study introduces a novel Arctic-focused remote sensing foundation model (RSFM) by combining diversity-aware regional-scale image curation with masked autoencoder (MAE) self-supervised pretraining of a Vision Transformer (ViT) encoder for very-high-spatial-resolution (VHSR) satellite image analysis.

**摘要：** 本研究引入了一种全新的北极遥感基础模型（RSFM），该模型结合了具有多样性意识的区域尺度图像筛选与视觉Transformer（ViT）编码器的掩码自编码器（MAE）自监督预训练，旨在进行超高空间分辨率（VHSR）卫星图像分析。

Spectral and acquisition-metadata descriptors were used in a scalable affinity-propagation clustering workflow to select approximately 3 million chips from 267 TB of Vantor VHSR imagery. This curation strategy was designed to reduce oversampling of visually repetitive or low-information areas while preserving broad scene diversity across the study domain.

研究利用光谱和采集元数据描述符，通过可扩展的亲和传播聚类工作流，从 267 TB 的 Vantor VHSR 影像中筛选出约 300 万个切片。这种筛选策略旨在减少对视觉重复或低信息区域的过采样，同时保持研究领域内广泛的场景多样性。

We pretrained a ViT-Large encoder on the curated corpus using a domain-adapted MAE reconstruction objective, producing Arctic-specific transformer weights for downstream feature mapping. The pretrained encoder was integrated into an existing location-aware detection and segmentation framework and evaluated across four hand-labeled Arctic datasets.

我们使用领域自适应的 MAE 重建目标，在筛选后的语料库上预训练了一个 ViT-Large 编码器，从而生成了用于下游特征映射的北极专用 Transformer 权重。该预训练编码器被集成到一个现有的位置感知检测与分割框架中，并在四个手工标注的北极数据集上进行了评估。

Compared to ImageNet-initialized ViT-Large baseline, Arctic MAE pretraining produced consistent improvements in foreground mean F1 scores of 0.87, 0.72, 0.93, and 0.87, for infrastructure, IWP, RTS, and TCNs, with approximately 5-8 percentage increase. The proposed model also outperformed Prithvi-EO-2.0 in all downstream comparisons, with the smallest gain corresponding to at least a 15 percentage improvement in mean F1, suggesting that domain-specific self-supervised pretraining on curated Arctic VHSR imagery provides more transferable representations for fine-scale Arctic mapping than a general-purpose Earth observation foundation model.

与基于 ImageNet 初始化的 ViT-Large 基线相比，北极 MAE 预训练在基础设施（infrastructure）、冰楔多边形（IWP）、热喀斯特塌陷（RTS）和苔原植被（TCNs）的前景平均 F1 分数上分别取得了 0.87、0.72、0.93 和 0.87 的稳定提升，增幅约为 5-8 个百分点。该模型在所有下游对比中均优于 Prithvi-EO-2.0，最小增幅也达到了平均 F1 分数 15 个百分点的提升。这表明，在经过筛选的北极 VHSR 影像上进行领域专用自监督预训练，比通用地球观测基础模型能为北极精细化制图提供更具可迁移性的表征。

These results demonstrate that optimizing the pretraining data distribution at regional scale, while keeping the architecture and MAE objective fixed, can produce a reusable Arctic-domain encoder for multiple VHSR remote sensing applications.

这些结果证明，在保持架构和 MAE 目标不变的情况下，优化区域尺度的预训练数据分布，可以产生一个适用于多种 VHSR 遥感应用的可复用北极领域编码器。