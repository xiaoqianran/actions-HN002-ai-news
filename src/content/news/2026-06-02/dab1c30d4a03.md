---
title: "Ensuring Data Integrity with Cryptographic Hashing and the Ethereum Blockchain"
originalUrl: "https://towardsdatascience.com/ensuring-data-integrity-with-cryptographic-hashing-and-the-ethereum-blockchain/"
date: "2026-06-01T23:22:48.034Z"
---

# Ensuring Data Integrity with Cryptographic Hashing and the Ethereum Blockchain
# 利用加密哈希与以太坊区块链确保数据完整性

**Data Engineering: Ensuring Data Integrity with Cryptographic Hashing and the Ethereum Blockchain**
**数据工程：利用加密哈希与以太坊区块链确保数据完整性**

Applying blockchain primitives to dataset versioning, provenance, and integrity assurance.
将区块链原语应用于数据集版本控制、来源追踪及完整性保证。

In many data science workflows, teams often need access to a shared dataset that stays perfectly synchronized and cannot be modified, e.g., in distributed machine learning environments where multiple teams rely on the exact same feature set. In this article, I’ll walk through a simple, fee-free method for cryptographically hashing a dataset of any size and storing its hash immutably on the Ethereum blockchain, creating a permanent and verifiable record of the dataset’s integrity. This method can also simply be extended to model weights, specific transformations which need to be applied in a consistent way, source code, or other data which needs to be immutable and verifiable.
在许多数据科学工作流程中，团队通常需要访问一个保持完美同步且不可篡改的共享数据集，例如在多个团队依赖完全相同特征集的分布式机器学习环境中。在本文中，我将介绍一种简单且免费的方法，用于对任意大小的数据集进行加密哈希处理，并将该哈希值不可篡改地存储在以太坊区块链上，从而为数据集的完整性创建一个永久且可验证的记录。该方法还可以轻松扩展到模型权重、需要以一致方式应用的特定转换、源代码或其他需要保持不可篡改且可验证的数据。

### 🤔 Why Integrity Matters
### 🤔 为什么完整性至关重要

If you’re at least somewhat familiar with data science as a practice, you’re already aware of the importance of data integrity. Even small changes or errors in the input data can collapse a project. Modern machine learning models are extremely sensitive to their training data. Missing normalization steps, a modified CSV file, shuffled rows, corrupted features, or mismatches between training and validation datasets can produce dramatically different results. Integrity failures are difficult to detect and often derailing. Models may still appear to function normally or train, but metrics can degrade slowly, drift accrues, or experiments become impossible to reproduce. Integrity is doubly important when the team is distributed, possibly across different organizations, and need to work on different versions of the same problem.
如果你对数据科学实践有一定了解，你一定知道数据完整性的重要性。输入数据中哪怕微小的变化或错误都可能导致项目崩溃。现代机器学习模型对训练数据极其敏感。缺失归一化步骤、修改过的 CSV 文件、打乱的行、损坏的特征，或者训练集与验证集之间的不匹配，都可能产生截然不同的结果。完整性故障难以检测且往往具有破坏性。模型可能看起来仍在正常运行或训练，但指标可能会缓慢下降、产生偏差，或者导致实验无法复现。当团队分布在不同地点（甚至不同组织）且需要处理同一问题的不同版本时，完整性显得尤为重要。

### 🔐 Using a Cryptographic Hash as a “Source of Truth”
### 🔐 使用加密哈希作为“事实来源”

A cryptographic hash gives us a simple and very useful mechanism for verifying data integrity. A brief primer on cryptographic hashes: A hash function takes any amount of input data (bytes) and deterministically produces a fixed length output known as a hash or digest. Cryptographic hashes are foundational in computer science, as you are most likely already aware. The key is determinism: Same data in → same hash out. Even a single byte changed in the input data produces a completely different hash. Because of this property, hashes act as unique fingerprints for data and are extremely useful for verifying integrity.
加密哈希为我们提供了一种简单且非常有用的验证数据完整性的机制。关于加密哈希的简要说明：哈希函数接收任意数量的输入数据（字节），并确定性地产生一个固定长度的输出，称为哈希值或摘要。正如你可能已经了解的那样，加密哈希是计算机科学的基础。其关键在于确定性：相同的输入数据 → 相同的哈希输出。输入数据中哪怕改变一个字节，都会产生完全不同的哈希值。由于这一特性，哈希值充当了数据的唯一指纹，对于验证完整性非常有用。

How does this apply to datasets? Because of the hash function’s determinism, once applied to a dataset, we can quickly and reliably test whether the dataset is identical to what we are expecting. This is exceptionally valuable with large datasets which are used by multiple teams, multiple companies, moving from one version to the next. Team 1 at Research Group Alpha creates features 1-10, Team 2 at Research Group Zeta creates features 10-100, System X consumes version Y, etc. We no longer need to question the data, simply compute the hash function over the dataset and compare it to the hash computed at a reference point. If it matches, OK. If not, something has changed. Hashing is extremely efficient. Running a hash function over a 10MB or 10TB dataset quickly gives us a small, fixed size string that can be shared, stored or published.
这如何应用于数据集？由于哈希函数的确定性，一旦应用于数据集，我们就可以快速且可靠地测试数据集是否与我们预期的一致。这对于被多个团队、多家公司使用，并不断从一个版本迭代到下一个版本的大型数据集来说，价值极高。例如，Alpha 研究小组的团队 1 创建了特征 1-10，Zeta 研究小组的团队 2 创建了特征 10-100，系统 X 使用版本 Y 等。我们不再需要质疑数据，只需对数据集计算哈希函数，并将其与参考点计算出的哈希值进行比较。如果匹配，则没问题；如果不匹配，说明数据发生了变化。哈希处理效率极高。对 10MB 或 10TB 的数据集运行哈希函数，能迅速得到一个小的、固定长度的字符串，该字符串可以被共享、存储或发布。

### 🧐 Why Use Ethereum as an Immutable Store?
### 🧐 为什么要使用以太坊作为不可篡改的存储？

This is the real useful piece of this article. Ethereum, again, as you are already aware is a blockchain. This gives us: Immutability: a transaction can never be changed; Distributed availability: always accessible without central authority; Permanent: once written, it is permanently accessible. But, Ethereum is for transactions? Don’t we need to write a complicated smart contract for this specialized purpose? You could indeed. But, we don’t need to. The clever bit is utilizing this uncommonly used input data field in an Ethereum transaction, sometimes referred to as “calldata.”
这是本文真正有用的部分。正如你所知，以太坊是一个区块链。它为我们提供了：不可篡改性（交易永远无法更改）、分布式可用性（无需中央机构即可随时访问）、永久性（一旦写入，即可永久访问）。但是，以太坊不是用于交易的吗？我们不需要为此专门编写复杂的智能合约吗？你确实可以这样做，但我们不需要。巧妙之处在于利用以太坊交易中一个不常用的输入数据字段，有时被称为“calldata”。

But, Ethereum transactions cost real money (gas, fees, etc)? Also true. On Ethereum, you are charged “gas” for each byte in the input data. On the mainnet, with a price of $2,000 per ETH, this might cost us between $0.04 – $0.10 per hash. This does not include the gas required for an actual transfer to be included by a block validator, which can be hefty depending on the network’s current load. Let’s make this more clever.
但是，以太坊交易需要真金白银（Gas 费等）吗？确实如此。在以太坊上，输入数据中的每个字节都会被收取“Gas”费。在主网上，假设 ETH 价格为 2,000 美元，每个哈希的成本可能在 0.04 到 0.10 美元之间。这还不包括区块验证者处理实际转账所需的 Gas 费，根据网络当前负载，这笔费用可能相当高昂。让我们换个更聪明的方法。

🦊 By offloading everything to the “testnet”, which every blockchain commonly has, we can make this entirely free. Sepolia (the ETH testnet) is rarely used unless you are a developer of smart contracts. Sepolia ETH is free and publicly available from faucets. This means we can create an infinite amount of transactions, on the publicly accessible testnet (called Sepolia for Ethereum), for free! As long as our input data is reasonably sized, Sepolia provides a way to utilize the blockchain for infinite data storage, with mostly the same properties as the mainnet.
🦊 通过将所有内容转移到每个区块链通常都有的“测试网”上，我们可以完全免费地实现这一点。Sepolia（以太坊测试网）除非你是智能合约开发者，否则很少使用。Sepolia ETH 是免费的，可以通过水龙头（faucets）公开获取。这意味着我们可以在公开可访问的测试网（以太坊的 Sepolia）上免费创建无限数量的交易！只要我们的输入数据大小合理，Sepolia 就提供了一种利用区块链进行无限数据存储的方法，且具备与主网基本相同的属性。

*Sepolia blockchains aren’t permanent, but are mostly trustable for multiple years. If you need absolute permanence, you’ll need to pay for it using the mainnet. Remember, we’re not storing the actual data on-chain. Just the fingerprint.
*Sepolia 区块链并非永久存在，但在未来几年内基本是可信的。如果你需要绝对的永久性，则需要使用主网并支付费用。请记住，我们并没有将实际数据存储在链上，只是存储了指纹。

### ⚙️ The process
### ⚙️ 流程

First, we need a way to reliably create transactions on Ethereum. Despite seeming complex, this is actually extremely simple. We don’t need any additional software or wallet tech. A wallet is nothing more than a key, paired with a secret used to sign it. To create an Ethereum transaction, we create a python object with the required keys and format, encode it with our key, and broadcast it to the network. A validator then picks up our transaction from the “mempool” and includes it in a block. As long as we include all the required fields, and it checks out, it’s now a permanent part of the blockchain within ~12 seconds.
首先，我们需要一种可靠的方法在以太坊上创建交易。尽管看起来很复杂，但实际上非常简单。我们不需要任何额外的软件或钱包技术。钱包不过是一个密钥，配有一个用于签名的私钥。要创建以太坊交易，我们只需创建一个包含所需键值和格式的 Python 对象，用我们的密钥对其进行编码，然后将其广播到网络。验证者随后会从“内存池”（mempool）中获取我们的交易并将其包含在区块中。只要我们包含了所有必需的字段且校验通过，它就会在约 12 秒内成为区块链的永久组成部分。

Step 1: Create the key and secret with web3.py with a few lines of code.
第一步：使用 web3.py 通过几行代码创建密钥和私钥。