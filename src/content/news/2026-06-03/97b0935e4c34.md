---
title: "Exploring Income Patterns with Python Pandas, Matplotlib, and Seaborn"
originalUrl: "https://towardsdatascience.com/from-raw-census-data-to-powerful-insights-exploring-income-patterns-with-pandas-matplotlib-seaborn/"
date: "2026-06-02T23:23:02.657Z"
---

# Exploring Income Patterns with Python Pandas, Matplotlib, and Seaborn
# 使用 Python Pandas、Matplotlib 和 Seaborn 探索收入模式

**Data Science: A Beginner-Friendly Exploratory Data Analysis on the US Census Dataset**
**数据科学：针对美国人口普查数据集的入门级探索性数据分析**

Mahnoor Javed | Jun 2, 2026 | 13 min read
Mahnoor Javed | 2026年6月2日 | 阅读时长 13 分钟

When we talk about earnings and income, we tend to credit success to hard work and intelligence. Other times, we just assume that certain people got lucky, and despite their sub-standard education levels or lack of expertise, they were able to succeed in their profession and earn comfortably. The truth, however, lies somewhat in between these two extremes. Yes, some people do get lucky and become millionaires at a young age, but we also see people working hard to climb the ladder of their career, and putting in efforts wherever needed to boost up professionally, and thus increase their income.
当我们谈论收入时，往往会将成功归功于勤奋和智慧。有时，我们也会认为某些人只是运气好，尽管他们的教育水平一般或缺乏专业知识，却依然能在职业生涯中取得成功并获得可观的收入。然而，事实往往介于这两个极端之间。确实，有些人很幸运，年纪轻轻就成了百万富翁，但我们也看到许多人通过努力工作在职业阶梯上攀升，并在需要的地方投入精力以提升专业能力，从而增加收入。

In this article, we will use Python to explore the relationship of income with different factors, namely age, gender, profession, level of education, etc. Although in today’s age, we can do the analysis with AI, plotting graphs and deriving insights, it is very important that we know how to extract insights from raw data by combining the humanly analysis with computing power. This is a beginner-level Python tutorial that requires certain prerequisite knowledge of Python fundamentals. By using Python and its powerful data processing libraries, we will identify some predictable patterns that will help us derive insights into factors that influence income generally, according to the dataset we will use!
在本文中，我们将使用 Python 来探索收入与不同因素（如年龄、性别、职业、教育水平等）之间的关系。尽管在当今时代，我们可以利用人工智能进行分析、绘制图表并得出结论，但了解如何将人类分析与计算能力相结合，从原始数据中提取见解依然至关重要。这是一个入门级的 Python 教程，需要一定的 Python 基础知识。通过使用 Python 及其强大的数据处理库，我们将识别出一些可预测的模式，这些模式将帮助我们根据所使用的数据集，深入了解影响收入的普遍因素！

### The Project
### 项目简介

In this project, we will dive into a census dataset with the help of Python, and use some of its powerful data analysis libraries like pandas, matplotlib, and seaborn, to uncover income patterns. With the help of data cleaning tools, data visualization, and exploratory analysis, we will convert this raw data into valuable insights about what factors influence income and to what extent. This is a beginner to intermediate level Python programming project that expects you to know about the basic Python fundamentals, especially how to import and use functions from different libraries for data exploration and analysis.
在这个项目中，我们将借助 Python 深入研究人口普查数据集，并使用 pandas、matplotlib 和 seaborn 等强大的数据分析库来揭示收入模式。通过数据清洗工具、数据可视化和探索性分析，我们将把这些原始数据转化为有价值的见解，了解哪些因素影响收入以及影响程度如何。这是一个初中级的 Python 编程项目，要求你掌握基本的 Python 基础知识，特别是如何导入和使用不同库中的函数来进行数据探索和分析。

### The Dataset
### 数据集

In this project, we will use the Adult Census Income Dataset, which is a real-world dataset derived from US census data. Although this dataset dates back to the 1990s, we can use it to derive income patterns with the margin that things have changed in 30 years, especially in terms of the gender gap that was previously very dominant. This dataset contains demographic and employment-related information, including the person’s age, occupation, level of education, marital status, gender, working hours etc, most of which are valuable to the purpose of our project.
本项目将使用“成人人口普查收入数据集”（Adult Census Income Dataset），这是一个源自美国人口普查数据的真实数据集。虽然该数据集可追溯到 20 世纪 90 年代，但我们仍可以用它来推导收入模式，同时考虑到 30 年来情况已经发生了变化，特别是在曾经非常显著的性别差距方面。该数据集包含人口统计和就业相关信息，包括个人的年龄、职业、教育水平、婚姻状况、性别、工作时长等，其中大部分对我们的项目目标非常有价值。

### Initializing the Coding Environment
### 初始化编程环境

Before we begin, let us make sure that we have our coding environment properly set up. For this, ensure that Python is installed in your system and open a programming IDE of your choice. I will be using PyCharm for its beginner-friendly nature and package accessibility. First, let us create a new project called “Adults Income Pattern Analysis” and create a Python file main.py. This is where we will do our coding.
在开始之前，请确保我们的编程环境已正确设置。为此，请确保你的系统中已安装 Python，并打开你选择的编程 IDE。我将使用 PyCharm，因为它对初学者友好且包管理方便。首先，创建一个名为“Adults Income Pattern Analysis”的新项目，并创建一个名为 main.py 的 Python 文件。我们将在这里编写代码。

### Installing and Importing Relevant Libraries
### 安装并导入相关库

Next, let us install the relevant libraries/Python packages. We will be using the following libraries for data exploration and analysis:
接下来，让我们安装相关的库/Python 包。我们将使用以下库进行数据探索和分析：

*   **Pandas** – this is one of the most popular libraries that helps one work with tabular data like CSV files.
*   **Pandas** – 这是最流行的库之一，有助于处理 CSV 文件等表格数据。
*   **Matplotlib** – this Python library allows one to create graphs, charts, and other data visualizations.
*   **Matplotlib** – 这个 Python 库允许用户创建图形、图表和其他数据可视化内容。
*   **Seaborn** – this is a library built on matplotlib, that extends data visualization to a large degree, making charts and graphs easier to make and prettier.
*   **Seaborn** – 这是一个基于 matplotlib 构建的库，它在很大程度上扩展了数据可视化功能，使图表的制作变得更简单、更美观。

We will install the above libraries using the terminal option in PyCharm:
我们将使用 PyCharm 中的终端选项安装上述库：

`pip install pandas matplotlib seaborn`

Once the installation is complete, we will go forward and import these libraries into our main.py file.
安装完成后，我们将继续在 main.py 文件中导入这些库。

```python
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
```

### Loading the Dataset & Basic Analysis
### 加载数据集与基础分析

Now, we will load the dataset as a dataframe in a variable called df. This is the standard pandas library way of loading the dataset into a dataframe to use ahead:
现在，我们将把数据集加载为一个名为 df 的数据框（dataframe）。这是 pandas 库加载数据集以供后续使用的标准方式：

```python
df = pd.read_csv("https://huggingface.co/api/resolve-cache/datasets/scikit-learn/adult-census-income/fbeef6ec0e6fd88a5028b94683144000a6b380d5/adult.csv")
print(df.head())
```

As you can see, we first loaded the dataset from the URL shared before, and then used the df.head() function to print out the first 5 rows of the dataset to get an idea of what it looks like. We can see some column names: age, workclass, fnlweight, hours.per.week, native.country and income.
如你所见，我们首先从之前分享的 URL 加载了数据集，然后使用 df.head() 函数打印出数据集的前 5 行，以了解其外观。我们可以看到一些列名：age（年龄）、workclass（工作类别）、fnlweight（最终权重）、hours.per.week（每周工作时长）、native.country（原籍国）和 income（收入）。

Now, let us see how many rows and columns our dataset has. We will do this using the df.shape command that will output the number of rows and columns, so we get an idea of how big the dataset is that we are dealing with.
现在，让我们看看数据集有多少行和多少列。我们将使用 df.shape 命令，它会输出行数和列数，这样我们就能了解所处理的数据集有多大。

```python
print(df.shape)
```

Lastly, let us get the detailed version of the column, including the type of data they are storing:
最后，让我们获取列的详细信息，包括它们存储的数据类型：

```python
print(df.info())
```

As can be seen from the above results, our dataset is a mix of categorical and numerical features.
从上述结果可以看出，我们的数据集混合了分类特征和数值特征。