---
title: "Hierarchical Global Attention (HGA)"
originalUrl: "https://arxiv.org/abs/2606.30709"
date: "2026-07-01T22:53:49.158Z"
---

# Hierarchical Global Attention (HGA)

**Abstract:** Hierarchical Global Attention (HGA) is a drop-in replacement for dense causal attention in pretrained long-context transformers. HGA preserves the original checkpoint parameters: the pretrained $W_Q$, $W_K$, $W_V$, and $W_O$ projections remain unchanged, no calibration parameters are introduced, and no retraining is required.

**摘要：** 分层全局注意力（Hierarchical Global Attention, HGA）是一种可直接替换预训练长上下文 Transformer 模型中稠密因果注意力（dense causal attention）的方案。HGA 保留了原始检查点（checkpoint）的参数：预训练的 $W_Q$、$W_K$、$W_V$ 和 $W_O$ 投影矩阵保持不变，无需引入任何校准参数，也无需进行重新训练。

Applied to Qwen3-30B-A3B-Instruct-2507-FP8 on a single RTX 5090 (32GB), the patched model runs out of the box at a 64K-token context, where token-level K/V storage is not feasible on this hardware. Unlike previous sparse-attention methods, HGA performs hierarchical two-level routing. It first retrieves relevant chunks using compact RoPE-aware summaries and then refines the selection by routing only the most relevant groups before performing exact token-level attention.

在单张 RTX 5090（32GB 显存）上应用 Qwen3-30B-A3B-Instruct-2507-FP8 模型时，打补丁后的模型可以在 64K token 上下文下直接运行，而在此硬件上，传统的 token 级 K/V 存储方案是无法实现的。与以往的稀疏注意力方法不同，HGA 执行分层两级路由：首先利用紧凑的 RoPE 感知摘要检索相关数据块，然后在执行精确的 token 级注意力之前，通过仅路由最相关的组来细化选择。

This hierarchical retrieval significantly reduces the number of fetched tokens while preserving exact attention over the retrieved token set, making RAM- and NVMe-backed storage practical. The full historical token K/V resides in host RAM or NVMe storage, while only a small routed working set is transferred to GPU memory during attention. Consequently, GPU memory consumption depends primarily on model weights and the routed working set rather than on the total context length.

这种分层检索显著减少了获取的 token 数量，同时在检索到的 token 集合上保持了精确的注意力计算，使得基于 RAM 和 NVMe 的存储方案变得切实可行。完整的历史 token K/V 存储在主机 RAM 或 NVMe 中，而在注意力计算期间，仅有少量路由后的工作集被传输到 GPU 显存中。因此，GPU 的显存消耗主要取决于模型权重和路由后的工作集，而不是总的上下文长度。

Across all tested context lengths (4K - 64K tokens), routed attention remains within approximately $0.01$--$0.02$ nats of dense attention while the sparsity used is just about 3%. These results suggest that the approximation introduced by hierarchical routing is small, and that the remaining quality gap is likely dominated by long-context positional encoding rather than by the routing algorithm itself.

在所有测试的上下文长度（4K - 64K tokens）中，路由注意力的性能与稠密注意力相比仅有约 $0.01$--$0.02$ nats 的差距，而所使用的稀疏度仅为 3% 左右。这些结果表明，分层路由引入的近似误差很小，剩余的质量差距很可能主要源于长上下文位置编码，而非路由算法本身。