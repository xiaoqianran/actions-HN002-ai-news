---
title: "Scaling Laws for Grid-Based Approximate Nearest Neighbor Search in High Dimensions"
originalUrl: "https://arxiv.org/abs/2607.01283"
date: "2026-07-03T22:36:18.655Z"
---

# Scaling Laws for Grid-Based Approximate Nearest Neighbor Search in High Dimensions
# 高维空间中基于网格的近似最近邻搜索的缩放定律

**Abstract:** Grid-based approaches to approximate nearest neighbor (ANN) search have been absent from modern scaling analyses. We present a systematic characterization of a multiprobe grid algorithm with respect to dataset size $N$ and dimensionality $d$. 

**摘要：** 基于网格的近似最近邻（ANN）搜索方法在现代缩放分析中一直处于缺失状态。我们针对数据集规模 $N$ 和维度 $d$，对一种多探针（multiprobe）网格算法进行了系统性的表征。

Our experiments reveal a previously unreported $d$-scaling crossover on the GloVe embedding family, in which multiprobe grid search maintains an approximately constant dimensional scaling exponent while other graph-, tree-, and partitioning-based methods exhibit degrading throughput. 

我们的实验揭示了 GloVe 嵌入系列中一种此前未被报道的 $d$-缩放交叉现象：多探针网格搜索保持了近似恒定的维度缩放指数，而其他基于图、树和划分的方法则表现出吞吐量的下降。

The advantage comes with near-linear query scaling in $N$, but also with lower indexing cost than competing ANN methods. Our results suggest that grid-based methods such as multiprobe grid may be competitive in rebuild-heavy or high-dimensional settings where indexing cost and dimensional robustness dictate performance. 

这一优势伴随着 $N$ 的近线性查询缩放，同时其索引成本也低于其他竞争性的 ANN 方法。我们的研究结果表明，在索引成本和维度鲁棒性决定性能的重构密集型或高维场景中，诸如多探针网格等基于网格的方法可能具有竞争力。

More broadly, recent work has formalized self-attention as an ANN operation. Thus, the $N$- and $d$-scaling properties of ANN algorithms may guide cost analysis of efficient transformer architectures. Code is available at: this https URL.

更广泛地说，近期的研究已将自注意力机制形式化为一种 ANN 操作。因此，ANN 算法的 $N$ 和 $d$ 缩放特性可以为高效 Transformer 架构的成本分析提供指导。代码地址：[点击此处](https://arxiv.org/abs/2607.01283)。