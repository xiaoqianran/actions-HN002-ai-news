---
title: "Manifest"
originalUrl: "https://www.producthunt.com/products/manifest-363"
date: "2026-07-21T22:36:30.849Z"
---

# Manifest

Launching today  
今日发布

Turn any webpage into an action manifest for AI agents  
将任意网页转换为 AI 代理的操作清单

147 followers  
147 位关注者

Visit website  
访问网站

**Browser Automation • LLM Developer Tools**  
**浏览器自动化 • 大语言模型开发者工具**

Manifest turns any webpage into a structured JSON map of what an AI agent can click, fill, and submit.  
Manifest 将任意网页转换为结构化 JSON 映射，清晰标注 AI 代理可点击、填写和提交的元素。

One API call, no fragile selectors.  
一次 API 调用，无需脆弱的定位器。

Every action comes with resolved CSS/role locators, plus a `requires` field that encodes dependencies between elements (e.g. "select a plan before this button is clickable").  
每个操作都附带解析后的 CSS/role 定位器，以及一个 `requires` 字段，用于编码元素间的依赖关系（例如：“在选择方案前，此按钮不可点击”）。

That's context aria snapshots don't give you.  
这是上下文 aria 快照无法提供的信息。

Python SDK, LangChain support, and an MCP server included.  
包含 Python SDK、LangChain 支持以及 MCP 服务器。

Built for anyone shipping browser agents.  
专为开发浏览器代理的团队打造。

---

**Launch Tags:**  
**发布标签：**  
API • Developer Tools • Artificial Intelligence  
API • 开发者工具 • 人工智能