---
title: "Grabette: an open system to record robot-manipulation data"
originalUrl: "https://huggingface.co/blog/grabette"
date: "2026-07-21T22:42:41.404Z"
---

# Grabette: an open system to record robot-manipulation data

**Grabette：一个用于记录机器人操作数据的开放系统**

---

Record your own manipulation tasks in minutes with a handheld gripper, turn them into robot-ready datasets automatically, and help grow an open, collaborative dataset for robot learning.

只需几分钟，用手持夹爪记录您自己的操作任务，自动将其转换为机器人可用的数据集，并共同助力构建一个开放、协作的机器人学习数据集。

---

The bottleneck isn’t the model. It’s the data. Robot learning has a supply problem. We have capable policy architectures (transformer-based VLAs, diffusion and flow-matching policies, and even world models) and the GPUs to train them. What we lack is large, diverse, real-world manipulation data.

瓶颈不在于模型，而在于数据。机器人学习面临供给问题。我们拥有强大的策略架构（基于Transformer的视觉语言动作模型、扩散和流匹配策略，甚至世界模型）以及用于训练它们的GPU。我们缺乏的是大规模、多样化、真实世界的操作数据。

---

Teleoperating a robot to collect it can be expensive and demanding: first of all, it requires a robot. And depending on the teleoperation method, data collection can be tedious for the user if it takes hours and involve significant hardware and logistical challenges. That is difficult to scale with the wide variety of tasks and environnements required.

通过遥操作机器人来收集数据可能成本高昂且要求苛刻：首先，它需要一台机器人。而且，根据遥操作方式的不同，如果耗时数小时并涉及重大的硬件和后勤挑战，数据收集对用户来说可能非常繁琐。面对所需任务和环境的广泛多样性，这很难规模化。

---

But you don't need a robot to collect robot data. Just a human hand, a gripper, a camera, and a way to recover the 6-DoF trajectory of what the hand did. Capture the demonstration and you have data a robot can learn from. That's what we're releasing today: Grabette, an open, low-cost system for recording manipulation data.

但收集机器人数据并不需要机器人。只需要一只人手、一个夹爪、一个摄像头，以及一种方法来恢复手部动作的6自由度轨迹。捕捉一次演示，你就得到了机器人可以学习的数据。这就是我们今天发布的内容：Grabette，一个用于记录操作数据的开放、低成本系统。

---

Pick it up, record a task with your own hand, and get back a clean, robot-ready dataset. No robot, no lab, no teleop rig. And that's the bigger goal: if recording a demonstration is as easy as shooting a video, anyone can contribute. We want Grabette to seed a large, open, collaborative manipulation dataset. One no single lab could ever build alone.

拿起它，用自己的手记录一个任务，然后得到一个干净的、机器人可用的数据集。不需要机器人，不需要实验室，不需要遥操作设备。而更大的目标是：如果记录演示像拍摄视频一样简单，任何人都能贡献。我们希望Grabette能催生一个大规模、开放、协作的操作数据集。这是任何单个实验室都无法独自构建的。

---

Standing on the shoulders of UMI
Grabette is directly inspired by the Universal Manipulation Interface (UMI) from Stanford: a handheld gripper with a fisheye camera that records demonstrations "in the wild", recovers camera trajectories with SLAM, and trains visuomotor policies from them. UMI proved the recipe works.

站在UMI的肩膀上
Grabette直接受到斯坦福大学通用操作接口（UMI）的启发：一个带鱼眼摄像头的手持夹爪，可在“野外”记录演示，使用SLAM恢复相机轨迹，并从中训练视觉运动策略。UMI证明了这一方案是可行的。

---

Other (closed source) devices exists like Agibot's MEgo gripper, Genrobot's DAS gripper and Sunday Robotics skill capture glove. Our goal was to make it effortless to use, to get the barrier from "I have a task" to "I have a trained model" as low as possible.

其他（闭源）设备也存在，例如Agibot的MEgo夹爪、Genrobot的DAS夹爪和Sunday Robotics的技能捕捉手套。我们的目标是让它使用起来毫不费力，将“我有一个任务”到“我有一个训练好的模型”之间的门槛降到最低。

---

Grabette is built into the modern open ecosystem: LeRobot for datasets, the Hugging Face Hub for sharing, and a processing pipeline you run from your browser with nothing to install.

Grabette构建于现代开放生态系统之中：使用LeRobot处理数据集，使用Hugging Face Hub进行共享，以及一个无需安装、可直接在浏览器中运行的处理流水线。

---

Grabette is something anyone can build on a workbench, use in the field, and contribute data from.

Grabette是任何人都可以在工作台上搭建、在实地使用并贡献数据的设备。

---

Meet Grabette
We have been developing Grabette for months, and we feel it has become usable enough to share. We are excited to share it now with you!

认识Grabette
我们开发Grabette已有数月，我们认为它已经足够好用，可以分享了。我们现在非常兴奋能与您分享它！

---

Grabette is a handheld gripper instrumented with everything needed to reconstruct a manipulation demonstration. It carries two cameras, each with a distinct job. Splitting the two roles is deliberate: the cheap wide fisheye gives the policy the context-rich, wrist-camera-style view it needs, while the RGBD camera does the heavy lifting of robust 6-DoF tracking.

Grabette是一个手持夹爪，配备了重建操作演示所需的一切。它携带两个摄像头，每个摄像头有明确的职责。这种分工是刻意设计的：廉价的广角鱼眼摄像头为策略提供所需的、富含上下文的手腕摄像头视角，而RGBD摄像头则承担稳健的6自由度跟踪的重任。

---

And while Grabette records data during tasks performed by a user, it relies on its robotic counterpart to execute the movements it has learned after training. So, meet Gripette, the robotic arm end-effector twin of Grabette.

当Grabette记录用户执行任务时的数据时，它依赖于其机器人对应物来执行训练后学到的动作。所以，请认识Gripette，它是Grabette的机器人手臂末端执行器孪生兄弟。

---

The family shares the same hardware DNA:
- **Grabette**, the handheld demonstration device (camera + IMU + gripper, BOM cost ~490€)
- **Gripette**, the motorized gripper (camera + two servomotors, BOM cost ~120€) that closes the loop on a real or simulated robot arm

这个系列共享相同的硬件基因：
- **Grabette**，手持演示设备（摄像头 + IMU + 夹爪，物料清单成本约490欧元）
- **Gripette**，电动夹爪（摄像头 + 两个伺服电机，物料清单成本约120欧元），用于在真实或模拟的机器人手臂上闭环

---

Grabette hand held recording device (left) and Gripette robot gripper (right)
（左）Grabette手持记录设备，（右）Gripette机器人夹爪

---

Built for everyone
Everything is open source
Go check the repository!

为所有人而建
一切皆开源
快去查看代码仓库！

---

Hardware: CAD and production files for Grabette and Gripette
Capture service: the on-device Raspberry Pi software
Processing pipeline: run locally, or online via our Hugging Face Space
Example downstream stack: stock LeRobot training + the OpenArm evaluation, as a reference

硬件：Grabette和Gripette的CAD及生产文件
采集服务：设备上的树莓派软件
处理流水线：本地运行，或通过我们的Hugging Face Space在线运行
示例下游技术栈：标准LeRobot训练 + OpenArm评估，作为参考

---

Components. Standard sensors you can buy, no closed pipeline, no fork lock-in. A Raspberry Pi, a standard Pi camera, an off-the-shelf OAK-D depth camera, magnetic encoders. The whole point is that anyone can build one from parts you can just order.

组件。标准传感器，可直接购买，无闭源流水线，无分支锁定。一个树莓派，一个标准树莓派摄像头，一个现成的OAK-D深度摄像头，磁编码器。核心在于任何人都可以用你能直接订购的零件组装一台。

---

Robot-agnostic by design. Nothing in the capture or the data format assumes a particular arm. Demonstrations are stored as camera-local 6-DoF cartesian pose plus gripper state, the output is a standard LeRobot dataset on the Hugging Face Hub, so the same data can drive different robots and different learning methods. You will still need the matching Gripette gripper on your arm though.

设计上即机器人无关。采集过程或数据格式中没有任何部分假定特定的机械臂。演示存储为相机局部6自由度笛卡尔位姿加夹爪状态，输出是Hugging Face Hub上的标准LeRobot数据集，因此同一数据可以驱动不同的机器人和不同的学习方法。不过，你的手臂上仍然需要匹配的Gripette夹爪。

---

From your hand to a dataset, in two steps
This release enables anyone to go from "I want to demonstrate a task" to "I have a training-ready dataset" quickly and without prior expertise.

从你的手到数据集，只需两步
此次发布使任何人都能快速、无需先验专业知识地从“我想演示一个任务”变为“我有一个可用于训练的数据集”。

---

1. **Record**
Press the button, and data from the observation camera, the tracking camera (color, depth, and IMU), and the gripper’s encoder joint values are recorded simultaneously, using a single shared clock to ensure proper synchronization. Press the button again to stop the episode, and the data is saved locally on the Raspberry Pi.

1. **记录**
按下按钮，观测摄像头、跟踪摄像头（彩色、深度和IMU）以及夹爪编码器关节值的数据会同时被记录，使用一个共享时钟以确保正确同步。再次按下按钮停止回合，数据会保存在树莓派本地。

---

2. **Process, directly in your browser**
Open the Grabette dashboard in your browser. Select the episodes you want to add to the dataset, and with one click, post-processing begins.
- The episodes are uploaded to the HF Hub
- The grabette-slam space performs SLAM using RTAB-MAP library and verifies that the trajectory is correct (with no jumps or loss of tracking)
- The episodes are converted to LeRobot format
- A new dataset is uploaded to your space, where you can view the data for each episode using the Le Robot visualizer
Everything is now ready to start training!

2. **处理，直接在浏览器中**
在浏览器中打开Grabette仪表板。选择要添加到数据集的回合，点击一下，后处理即开始。
- 回合被上传到HF Hub
- grabette-slam空间使用RTAB-MAP库执行SLAM，并验证轨迹是否正确（无跳跃或跟踪丢失）
- 回合被转换为LeRobot格式
- 一个新数据集被上传到你的空间，你可以使用Le Robot可视化工具查看每个回合的数据
现在一切就绪，可以开始训练了！

---

What can you do with the da
（原文此处不完整）