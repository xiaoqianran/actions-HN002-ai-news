---
title: "LongCat-Video-Avatar 1.5 cuts inference to 8 steps — here's"
originalUrl: "https://dev.to/creeta/longcat-video-avatar-15-cuts-inference-to-8-steps-heres-152b"
date: "2026-07-21T23:04:28.595Z"
---

### Title: LongCat-Video-Avatar 1.5 cuts inference to 8 steps — here's  
**标题：LongCat-Video-Avatar 1.5 将推理步骤削减至8步 — 以下是**

---

Meituan's LongCat team shipped a quiet but meaningful update to its open-source talking-avatar stack: version 1.5 swaps the audio encoder, distills sampling down to 8 steps, and adds an INT8 path to fit the model on tighter GPUs. Here's what actually changed under the hood, and which flags you now have to pass.  
美团 LongCat 团队为其开源对话虚拟人技术栈发布了一次低调但有意义的更新：1.5 版本更换了音频编码器，将采样蒸馏至 8 步，并增加了 INT8 路径以适应更紧凑的 GPU。以下是其内部实际的变化，以及你现在需要传递的标志。

---

LongCat-Video-Avatar 1.5: DMD2 distillation, Wav2Vec2 replacement, and INT8 offloading  
LongCat-Video-Avatar 1.5：DMD2 蒸馏、Wav2Vec2 替换与 INT8 卸载

LongCat-Video-Avatar 1.5 is an audio-driven talking-avatar generator that turns one portrait plus an audio clip into a lip-synced video with head motion, expression, and body dynamics. Released May 21, 2026, it is an avatar head built on the 13.6-billion-parameter dense LongCat-Video diffusion transformer [base model, Oct 2025].  
LongCat-Video-Avatar 1.5 是一个音频驱动的对话虚拟人生成器，可将一张肖像照片和一段音频剪辑转换为包含头部运动、表情和身体动态的口型同步视频。该版本于 2026 年 5 月 21 日发布，是基于 136 亿参数密集型 LongCat-Video 扩散Transformer [基础模型，2025年10月] 构建的虚拟人头部。

The headline change: v1.5 replaces the Wav2Vec2 audio encoder used in v1.0 (Dec 16, 2025) with Whisper-Large-v3, which the team attributes to smoother, more natural lip dynamics.  
主要变化：v1.5 用 Whisper-Large-v3 替换了 v1.0（2025年12月16日）中使用的 Wav2Vec2 音频编码器，团队认为这带来了更平滑、更自然的唇部动态。

Three new flags define the v1.5 workflow, none of which exist in v1.0:  
三个新标志定义了 v1.5 的工作流，v1.0 中均不存在：

| Flag | What it does | Notes |
|------|--------------|-------|
| --use_distill | 启用基于 DMD2 的步数蒸馏，将生成压缩至 8 NFE | v1.5 强制要求 — 省略它将回退到完整链路的 v1.0 采样计划，而非 8 步路径 |
| --use_int8 | 以 INT8 加载 136 亿参数的密集 DiT 以降低显存压力 | 消费级 GPU 的主要调节杠杆 |
| --resolution | 选择 480P 或 720P 输出 | 1.5 版本新增 |

---

Under the hood, the technique report describes Cross-Chunk Latent Stitching, which removes redundant VAE decode/encode cycles between autoregressive chunks, enabling seamless minutes-long generation without re-encoding overhead [arXiv:2605.26486]. That matters for long-form output where earlier tools drift or stutter at chunk boundaries.  
在内部，技术报告描述了“跨块潜在拼接”，它消除了自回归块之间冗余的 VAE 解码/编码循环，实现了无需重新编码开销的分钟级无缝生成 [arXiv:2605.26486]。这对于长格式输出很重要，因为早期工具在块边界处会出现漂移或卡顿。

One caveat worth flagging before you invest a GPU-hour: Meituan claims parity-or-better results versus HeyGen, Kling Avatar 2.0, and OmniHuman-1.5 across 508 image-audio pairs, 770 crowdsourced evaluators, and 13,240 judgments [human-eval benchmark]. That evaluation is entirely author-controlled and reported as win-rates; no independent leaderboard corroborates the superiority claim yet. Treat it as vendor evidence, not a settled result.  
在投入 GPU 时间前，有一个值得注意的警告：美团声称在 508 个图像-音频对、770 名众包评估者和 13,240 次判断 [人工评估基准] 上，其结果与 HeyGen、Kling Avatar 2.0 和 OmniHuman-1.5 相当或更优。该评估完全由作者控制并以胜率报告；尚无独立排行榜证实其优越性声明。将其视为供应商证据，而非定论。

Credit to the source video that surfaced this release: YouTube walkthrough.  
感谢 surfaced 此次发布的源视频：YouTube 演示。

---

Preparing your environment for LongCat-Video-Avatar 1.5: PyTorch 2.6.0, CUDA 12.4, FlashAttention-2  
为 LongCat-Video-Avatar 1.5 准备环境：PyTorch 2.6.0, CUDA 12.4, FlashAttention-2

Setup begins by cloning the base repository — the avatar is a head on top of the LongCat-Video foundation model, so you install that first and add the avatar layer on top. Create a Python 3.10 virtual environment before anything else; the pinned FlashAttention-2 wheel is not tested against 3.11+, and version drift there is a common early failure.  
设置从克隆基础仓库开始 — 虚拟人是基于 LongCat-Video 基础模型构建的头部，因此你先安装基础模型，再在其上添加虚拟人层。首先创建 Python 3.10 虚拟环境；固定版本的 FlashAttention-2 wheel 未针对 3.11+ 测试，版本漂移是常见的早期失败原因。

The dependency the setup most often breaks on is FlashAttention-2 v2.7.4.post1, a hard requirement. Install it from the wheel that matches CUDA 12.4 against PyTorch 2.6.0 — a CUDA mismatch with the default pip build is the single most frequent install error.  
设置最常出错的依赖是 FlashAttention-2 v2.7.4.post1，这是一个硬性要求。从匹配 CUDA 12.4 和 PyTorch 2.6.0 的 wheel 安装它 — 与默认 pip 构建的 CUDA 不匹配是单一最常见的安装错误。

Then install both requirements files: requirements.txt for the base model and requirements_avatar.txt for the avatar path, which pulls librosa and expects ffmpeg on the system.  
然后安装两个需求文件：基础模型的 requirements.txt 和虚拟人路径的 requirements_avatar.txt，后者会拉取 librosa 并期望系统已安装 ffmpeg。

Pull the weights with `huggingface-cli download meituan-longcat/LongCat-Video-Avatar-1.5`. Note the DMD2 distill LoRA — roughly 1.26 GB in the Comfy-packaged form — is a separate artifact and is required for the 8-NFE distilled path, not an optional extra.  
使用 `huggingface-cli download meituan-longcat/LongCat-Video-Avatar-1.5` 拉取权重。注意 DMD2 蒸馏 LoRA — Comfy 打包形式约 1.26 GB — 是一个独立构件，对于 8-NFE 蒸馏路径是必需的，而非可选附加项。

The reference launch uses `torchrun --nproc_per_node=2` with `--context_parallel_size=2`, i.e. two GPUs in context-parallel. Single-GPU runs are feasible by adding `--use_int8` to cut VRAM on the 13.6B dense base, but that is not the configuration the official docs test — expect to tune offloading yourself.  
参考启动命令使用 `torchrun --nproc_per_node=2` 配合 `--context_parallel_size=2`，即上下文并行使用两块 GPU。通过添加 `--use_int8` 减少 136 亿参数密集基础模型的显存，单 GPU 运行是可行的，但官方文档未测试此配置 — 预期需要自行调整卸载。

---

Producing a talking-head avatar with LongCat-Video-Avatar 1.5: torchrun, AT2V, and DMD2  
使用 LongCat-Video-Avatar 1.5 生成说话头像：torchrun、AT2V 与 DMD2

LongCat-Video-Avatar 1.5 exposes four task modes, and picking the right one decides how tightly identity is preserved. AT2V (Audio-Text-to-Video) generates a talking clip from an audio track plus a text prompt with no reference image. ATI2V/AI2V add a portrait (Audio-Text-Image or Audio-Image-to-Video), which routes through Reference Skip Attention to hold the face steady without copy-paste artifacts or identity drift. Prefer ATI2V or AI2V when you have a source photo and care about consistency.  
LongCat-Video-Avatar 1.5 公开了四种任务模式，选择正确的模式将决定身份保留的紧密程度。AT2V（音频-文本-视频）从音轨加文本提示（无参考图像）生成说话片段。ATI2V/AI2V 增加肖像（音频-文本-图像或音频-图像-视频），通过参考跳跃注意力保持面部稳定，避免复制粘贴伪影或身份漂移。当有源照片且关注一致性时，优先选择 ATI2V 或 AI2V。

The fourth mode is audio-conditioned continuation, covered below.  
第四种模式是音频条件延续，如下所述。

A single-speaker AT2V run looks like this — the snippet is verified and prints the exact upstream CLI it wraps rather than executing the model:  
单说话人 AT2V 运行如下 — 此片段经验证，仅打印其包装的上游 CLI 而非执行模型：

```python
"""Minimal LongCat-Video-Avatar 1.5 8-step inference demo.
This prints the distilled inference settings and the matching upstream CLI.
Running the real model requires downloading the weights and LongCat-Video repo.
"""
MODEL = "meituan-longcat/LongCat-Video-Avatar-1.5"

def main():
    steps = 8
    command = [
        "torchrun",
        "--nproc_per_node=2",
        "run_demo_avatar_single_audio_to_video.py",
        "--context_parallel_size=2",
        "--checkpoint_dir=./weights/LongCat-Video-Avatar-1.5",
        "--stage_1=at2v",
        "--input_json=assets/avatar/single_example_1.json",
        "--use_distill",
        "--model_type=avatar-v1.5",
        "--use_int8",
    ]
    print(f"{MODEL} uses distilled inference: {steps} steps")
    print(" ".join(command))

if __name__ == "__main__":
    main()
```

Both `--use_distill` and `--model_type avatar-v1.5` are mandatory for the 8-step (8-NFE) distilled path. Set Audio CFG in the 3–5 range per the official docs: below 3 under-animates the mouth, above 5 over-exaggerates it.  
对于 8 步（8-NFE）蒸馏路径，`--use_distill` 和 `--model_type avatar-v1.5` 均为强制要求。根据官方文档，将音频 CFG 设置在 3-5 范围内：低于 3 会使嘴部动画不足，高于 5 会过度夸张。

For two speakers, run `run_demo_avatar_multi_audio_to_video.py`. Use `--audio_type para` for equal-length clips (parallel merge, summed) or `--audio_type add` for unequal clips sequenced with silence padding; person1/person2 ordering is positional. In parallel-merge mode, L-ROPE-based audio-visual binding supplies spatial speaker separation so voices do not bleed identity between the two faces.  
对于两个说话人，运行 `run_demo_avatar_multi_audio_to_video.py`。对等长片段使用 `--audio_type para`（并行合并、求和），对不等长片段使用 `--audio_type add`（以静音填充序列化）；person1/person2 顺序是位置性的。在并行合并模式下，基于 L-ROPE 的音频-视觉绑定提供空间说话人分离，使声音不会在两个面部之间混淆身份。

Video continuation feeds a reference clip plus new audio to extend an existing segment. Here Cross-Chunk Latent Stitching removes the redundant VAE decode/encode cycle between autoregressive chunks, so the boundary seam stays c  
视频延续通过输入参考片段加新音频来扩展现有片段。此处跨块潜在拼接消除了自回归块之间冗余的 VAE 解码/编码循环，因此边界接缝保持 c