---
title: "SIMD for Collision"
originalUrl: "https://box2d.org/posts/2026/07/simd-for-collision/"
date: "2026-07-22T23:26:09.954Z"
---

# SIMD for Collision / SIMD 与碰撞检测

SIMD for Collision In a previous post SIMD Matters I discussed the big wins I got from graph coloring to make the contact solver faster. That same approach exists in Box3D. I like to call this approach “wide SIMD”. The idea is to process multiple work units at the same time. In the contact solver this means solving four contact points at the same time. This is different from “narrow SIMD” where a 3-vector (xyz) is put into a SIMD register and then standard vector math is expressed as SIMD intrinsics. Narrow SIMD can be useful, but the gains are not as obvious. In 3D there is an opportunity for using wide SIMD that doesn’t exist in 2D.

在之前的文章《SIMD Matters》中，我讨论了通过图着色显著提升接触求解器速度的方法。同样的方法也存在于 Box3D 中。我喜欢将这种方法称为“宽 SIMD”。其核心思想是同时处理多个工作单元。在接触求解器中，这意味着同时求解四个接触点。这与“窄 SIMD”不同，后者将三维向量（xyz）放入 SIMD 寄存器，并将标准向量运算表达为 SIMD 内部函数。窄 SIMD 可能有用，但性能提升并不那么明显。在 3D 中，存在 2D 所没有的使用宽 SIMD 的机会。

Consider the convex pile benchmark ported from PEEL. This benchmark drops 5120 convex hulls, each with 32 points. Box3D treats boxes as hulls and many of the benchmarks use boxes. The performance with boxes has been good and typically most of the cost is not in the narrow phase. But the 32 point hull is a different beast. Let’s call it “boulder”. The table below shows the hull details for a box compared to the boulder.

考虑从 PEEL 移植的凸堆基准测试。该基准测试投放 5120 个凸包，每个凸包有 32 个点。Box3D 将盒子视为凸包，许多基准测试使用盒子。盒子的性能一直很好，通常大部分成本不在窄相位。但 32 点凸包是另一种情况。我们称之为“巨石”。下表比较了盒子与巨石的凸包细节。

Hull | Vertices | Faces | Edges
--- | --- | --- | ---
Box | 8 | 6 | 12
Boulder | 32 | 59 | 89

Box3D uses the Separating Axis Test for collision detection. This is abbreviated as SAT. Using SAT I can find the best features to use for pushing the shapes apart and precisely how far apart they need to be pushed to remove overlap. Furthermore, I use these results to compute the contact normal and the contact points. Other physics engines may use the distance algorithm GJK instead with an overlap fallback such as the Expanding Polytope Algorithm, also known as EPA. I’m a big fan of SAT for convex hull collision because it doesn’t require a collision margin. Shapes can rest directly on each other. The GJK and EPA combo usually tries to keep shapes slightly separated to make keep the shapes in the GJK region, which is faster. This can lead to visual gaps. Also, EPA can be numerically brittle and often physics engines will need a fallback if EPA fails: a fallback for the fallback. EPA is essentially an algorithm for computing a convex hull, and the input data can be flat slivers. That is a challenging scenario for convex hull computation. Hence the need for a second fallback.

Box3D 使用分离轴测试（SAT）进行碰撞检测。使用 SAT，我可以找到用于将形状推开的最佳特征，以及精确推开多远以消除重叠。此外，我利用这些结果计算接触法线和接触点。其他物理引擎可能改用距离算法 GJK，并配合重叠回退方案，如扩展多面体算法（EPA）。我非常喜欢将 SAT 用于凸包碰撞，因为它不需要碰撞边距。形状可以直接相互接触。GJK 和 EPA 组合通常试图保持形状轻微分离，以保持在 GJK 区域内，这更快。但这可能导致视觉间隙。此外，EPA 可能在数值上脆弱，物理引擎通常需要在 EPA 失败时有一个回退方案：回退的回退。EPA 本质上是计算凸包的算法，输入数据可能是扁平细条。这是凸包计算中具有挑战性的场景。因此需要第二个回退。

Unfortunately, SAT in 3D has quadratic complexity. With two hulls A and B, the collision algorithm must test the faces of hull A against the vertices of hull B, the faces of hull B against the vertices of hull A, and the edges of hull A against the edges of hull B. In the worst case, every combination must be evaluated.

不幸的是，3D 中的 SAT 具有二次复杂度。对于两个凸包 A 和 B，碰撞算法必须测试凸包 A 的面与凸包 B 的顶点、凸包 B 的面与凸包 A 的顶点，以及凸包 A 的边与凸包 B 的边。在最坏情况下，必须评估所有组合。

Combination | Face-Vertex | Vertex-Face | Edge-Edge
--- | --- | --- | ---
Box-Box | 6 | 6 | 144
Boulder-Boulder | 59 | 59 | 7921

The number of edge-edge combinations grows quadratically. There are some tricks to speed up the edge tests using the Gauss Map. See Improvements to the Separating Axis Test for more details. Regardless, the edge-edge test can easily dominate the entire simulation. So what does this edge-edge code look like? At a basic level it looks like this:

边-边组合的数量呈二次增长。有一些使用高斯图加速边测试的技巧。更多细节请参见《分离轴测试的改进》。无论如何，边-边测试可能轻易主导整个模拟。那么这段边-边代码是什么样子？基本层面如下：

```cpp
for (Edge edgeA : hullA.edges) {
    for (Edge edgeB : hullB.edges) {
        TestCrossProduct(edgeA, edgeB);
    }
}
```

If there are just 12 edges in each hull, doing SIMD on this is not a big win because SIMD requires some setup work. For SIMD to work well the data needs to be in structure of arrays format (SoA). With 89 edges in each hull the story changes drastically. There are 7921 calls to TestCrossProduct. With wide SIMD I can test one edge of hullA against four edges of hullB simultaneously. It looks sort of like this:

如果每个凸包只有 12 条边，对此使用 SIMD 收益不大，因为 SIMD 需要一些设置工作。为了让 SIMD 高效工作，数据需要采用数组结构格式（SoA）。当每个凸包有 89 条边时，情况截然不同。有 7921 次 TestCrossProduct 调用。使用宽 SIMD，我可以同时测试凸包 A 的一条边与凸包 B 的四条边。看起来类似这样：

```cpp
for (Edge edgeA : hullA.edges) {
    for (EdgeWide edgeWideB : hullB.edgesWide) {
        TestCrossProductWide(edgeA, edgeWideB);
    }
}
```

There are a lot of hidden details in the snippet and you can look at the Box3D code if you are curious. But I’m here to answer the question: should we care about SIMD for collision detection? Well, I have results! These are the results for the convex pile benchmark with the total milliseconds for a 500 step run. I tested 1 to 8 threads on an AMD 7950x with the CPU pegged at 4.42GHz. The results are the best of 4 runs.

代码片段中有许多隐藏细节，如果好奇可以查看 Box3D 代码。但我在这里回答这个问题：我们应该关心碰撞检测中的 SIMD 吗？我有结果！这些是凸堆基准测试的结果，为 500 步运行的总毫秒数。我在 AMD 7950x 上测试了 1 到 8 个线程，CPU 锁定在 4.42GHz。结果是 4 次运行中的最佳值。

Threads | Scalar | SSE2 | AVX2-Lite
--- | --- | --- | ---
1 | 40706 | 17337 | 15762
2 | 20799 | 8857 | 8131
3 | 13789 | 5946 | 5471
4 | 10324 | 4509 | 4084
5 | 8359 | 3675 | 3361
6 | 6958 | 3106 | 2843
7 | 6006 | 2697 | 2477
8 | 5292 | 2410 | 2277

SSE2 is over twice as fast as scalar! Keep in mind these timings are for the entire simulation, not just the edge-edge test. The scalar column also indicates that the contact solver runs in scalar mode. So this is the full picture of the simulation. Box3D only implements SSE2 intrinsics. In Box2D I have AVX2 intrinsics as well, but there were a surprising number of users without AVX2 capable CPUs. Nevertheless, just enabling the AVX2 architecture in Box3D gives a nice gain, as indicated in the AVX2-Lite column. Free performance gains are great to see. In the future I may try a real AVX2 implementation testing 8 edges at a time. This benchmark represents a 32-point hull with 89 edges. Surely SAT will be overrun as the point count grows further. Well, fortunately Box3D has a hard 128 edge limit. This limit already existed and has to do with keeping the hull storage compact (8-bit indices and two half-edges per edge). Somewhat surprisingly, converting complex hulls to meshes can solve the quadratic growth problem, but it makes the shape less suitable for a dynamic body. You may wonder if the SIMD edge tests moved the needle on box-box. Sadly it has almost no effect. This optimization seems to only benefit complex hulls. Such hulls can come up in several scenarios, such as destruction. So it is a worthy addition to Box3D. Finally, here is a video comparison and a side-by-side of the frame timings.

SSE2 比标量模式快两倍以上！请记住这些时间是整个模拟的，不仅仅是边-边测试。标量列也表明接触求解器以标量模式运行。所以这是模拟的全貌。Box3D 仅实现了 SSE2 内部函数。在 Box2D 中我也实现了 AVX2 内部函数，但出乎意料地有许多用户没有支持 AVX2 的 CPU。尽管如此，仅在 Box3D 中启用 AVX2 架构就能带来不错的提升，如 AVX2-Lite 列所示。免费的性能提升很棒。未来我可能会尝试真正的 AVX2 实现，一次测试 8 条边。此基准测试代表有 89 条边的 32 点凸包。随着点数进一步增加，SAT  surely 会不堪重负。幸运的是，Box3D 有严格的 128 条边限制。此限制已存在，与保持凸包存储紧凑有关（8 位索引和每条边两个半边）。 somewhat 令人惊讶的是，将复杂凸包转换为网格可以解决二次增长问题，但会使形状不太适合动态刚体。你可能想知道 SIMD 边测试是否影响了盒子-盒子碰撞。遗憾的是它几乎无效。此优化似乎只对复杂凸包有益。此类凸包可能在几种场景中出现，如破坏。因此它是 Box3D 的有价值补充。最后，这是一个视频比较和并排的帧时间。