---
title: "Loop Engineering for RAG Generation: Iterate top-k One at a Time"
originalUrl: "https://towardsdatascience.com/loop-engineering-for-rag-generation-when-top-1-is-enough-when-you-need-top-k/"
date: "2026-07-22T23:51:02.523Z"
---

# Loop Engineering for RAG Generation: Iterate top-k One at a Time  
# RAG生成中的循环工程：一次迭代一个top-k  

Large Language Models Loop Engineering for RAG Generation: Iterate top-k One at a Time Enterprise Document Intelligence [Vol.1 #8bis] – Two regimes for sending retrieved candidates to the generation brick, the sufficiency signal that picks between them, and the per-question type dispatch that makes it cheap Kezhan Shi Jul 22, 2026 9 min read  
大语言模型RAG生成中的循环工程：一次迭代一个top-k《企业文档智能》[第1卷 #8bis] – 将检索候选结果发送到生成模块的两种机制、用于选择机制的充分性信号，以及使成本可控的按问题类型分发策略。作者：石克战，2026年7月22日，阅读时长9分钟  

The user asks “what is the effective date of this policy?”. Retrieval returns five candidate line-windows and passes them all to the LLM in one prompt. The LLM reads all five to extract the same date the first candidate already had. Chunks two to five were signatures, footnotes, and a paragraph about historical dates. Paid for nothing.  
用户提问“此政策的生效日期是什么？”。检索返回五个候选文本片段，并一次性全部输入大语言模型。模型读取全部五个片段，提取出的日期与第一个片段已包含的日期相同。第二至第五个片段分别是签名、脚注和一段关于历史日期的内容。纯属浪费。  

Send the top-1 first, ask the LLM if that is enough, and stop when it says yes: sequential feeding cuts the token cost by 80 % on this class of question.  
先发送top-1，询问大语言模型信息是否充足，得到肯定答复后停止：对于此类问题，顺序 feeding 可将令牌成本降低80%。  

The rest of the article catalogues where sequential wins, where a single-call over all K is the right default, and how the question parser dispatches between the two.  
本文其余部分将系统梳理顺序 feeding 的适用场景、一次性发送全部K个候选的默认正确场景，以及问题解析器如何在两者间进行分发。  

This article is a companion to Enterprise Document Intelligence, the series whose philosophy is laid out in Amplify the Expert, the series that builds enterprise RAG from four bricks (document parsing, question parsing, retrieval, generation). It sits between Article 8 (generation) and Article 9 (upgrading the mini-RAG) and develops one specific decision: how do you feed the top-K retrieved candidates into the generation brick?.  
本文是《企业文档智能》系列的 companion 文章。该系列哲学在《放大专家》中阐述，旨在通过四个模块（文档解析、问题解析、检索、生成）构建企业级RAG。本文位于第8篇（生成）与第9篇（升级迷你RAG）之间，聚焦一个具体决策：如何将top-K检索结果输入生成模块？  

The answer most pipelines have is “all K at once”. This article catalogues the second regime (sequential, top-1 first) and shows when each one wins.  
多数流水线的答案是“一次性全部输入K个”。本文梳理第二种机制（顺序、先top-1）并展示各自获胜场景。  

The naive baseline this article pushes back on  
本文反对的天真基线  

The two regimes for feeding the top-K to the generation brick – Image by author  
将top-K输入生成模块的两种机制 – 作者配图  

Naive RAG ships batch by default. Retrieval returns top-5, the LLM gets all 5, the answer comes back. It works, and on hard questions (comparison, listing) it is the right choice. But the silent cost is paid on every other question: the easy factual ones where the top-1 already had the answer.  
天真RAG默认采用批量模式。检索返回top-5，大语言模型接收全部5个，然后返回答案。这可行，对于难题（比较、列举）也是正确选择。但沉默成本发生在其他所有问题上：那些top-1已包含答案的简单事实类问题。  

This article walks the second regime and the dispatch that picks per question.  
本文详解第二种机制及按问题选择的分发策略。  

Where this article sits in the series: brick 8 (generation) highlighted – Image by author  
本文在系列中的位置：突出显示模块8（生成） – 作者配图  

📓 The runnable notebook for this article is on GitHub: doc-intel/notebooks-vol1. It sends the same questions through both batch and sequential generation, prints the per-question token cost and the two sufficiency booleans (answer_found, complete_answer_found) that stop the loop, and reproduces the dispatch table on your own machine.  
📓 本文配套可运行笔记本位于GitHub：doc-intel/notebooks-vol1。它用批量与顺序两种生成方式处理相同问题，打印每问题令牌成本及两个停止循环的充分性布尔值（answer_found, complete_answer_found），并在你的机器上复现分发表。  

The public companion-code repo at doc-intel/notebooks-vol1 – Image by author  
公开配套代码仓库：doc-intel/notebooks-vol1 – 作者配图  

1. Two regimes for feeding the top-K to generation  
1. 将top-K输入生成的两种机制  

Retrieval hands generation an ordered top-K. There are two ways to feed it in, and they cost very differently.  
检索向生成模块传递有序的top-K结果。有两种输入方式，成本差异显著。  

1.1 The fixed top-K pipeline and what it wastes  
1.1 固定top-K流水线及其浪费  

The default pattern across most RAG tutorials looks like this:  
大多数RAG教程的默认模式如下：  

top_k = retrieval(question, k=5)  
answer = generation(question, top_k)  

It runs in two steps for every question. Token cost is roughly generation_cost(question + 5 chunks of context). Latency is roughly retrieval + one LLM call. And the LLM happily processes the 5 chunks even when the top-1 was already sufficient and chunks 2..5 added zero information.  
每个问题分两步运行。令牌成本约为 generation_cost(问题 + 5个上下文片段)。延迟约为检索时间 + 一次大语言模型调用。即使top-1已足够，第2至5个片段零信息增量，模型仍会愉快处理全部5个片段。  

Concretely: the user asks “what is the effective date of this policy?”. Retrieval returns 5 line-windows where the keyword "effective" appears. The first one is the answer (“effective from January 1, 2026”). Chunks 2..5 are signatures, footnotes, and a paragraph about historical effective dates of past policies. The LLM reads all 5 to extract the same date the first one already had.  
具体而言：用户问“此政策的生效日期？”。检索返回5个包含关键词“effective”的文本行。第一个就是答案（“自2026年1月1日起生效”）。第2至5个是签名、脚注和一段关于过往政策历史生效日期的段落。模型读取全部5个，提取的日期与第一个已包含的完全相同。  

On a corpus of one document, the cost is rounding error. On a corpus of 50k policies, this is real money per month.  
在单个文档语料中，这只是零头误差。在5万份政策文档语料中，这每月都是真金白银的浪费。  

1.2 Sequential: top-1 first, sufficiency predicate, escalate if needed  
1.2 顺序模式：先top-1，充分性谓词，必要时升级  

The sequential regime treats the K candidates as an ordered list and asks the generation brick to validate sufficiency at each step:  
顺序机制将K个候选视为有序列表，并要求生成模块在每一步验证充分性：  

for i, candidate in enumerate(top_k):  
    answer = generation(question, [candidate])  
    if answer.answer_found and answer.complete_answer_found:  
        break  

The sufficiency signal lives inside the typed contract Article 8A introduced. The AnswerWithEvidence schema exposes answer_found (was the question’s information present in this candidate?) and complete_answer_found (was the entire answer present, not a fragment?). The loop reads those fields, not a custom heuristic.  
充分性信号存在于第8A篇引入的强类型合同中。AnswerWithEvidence 模式暴露 answer_found（该候选是否包含问题所需信息？）和 complete_answer_found（是否包含完整答案而非片段？）。循环读取这些字段，而非自定义启发式规则。  

In the effective date example above: generation runs once on the top-1 candidate, the LLM returns answer_found = True, complete_answer_found = True, the loop exits. Tokens spent: generation_cost(question + 1 chunk of context). That is 1/5 of the batch cost for this question, on a pipeline that handles thousands of similar lookups per day.  
在上述生效日期示例中：生成模块仅在top-1候选上运行一次，大语言模型返回 answer_found=True, complete_answer_found=True，循环退出。消耗令牌：generation_cost(问题 + 1个上下文片段)。对于每天处理数千次类似查询的流水线，这仅为批量成本的1/5。  

1.3 Batch: send all K at once, let the LLM arbitrate  
1.3 批量模式：一次性发送全部K个，由大语言模型仲裁  

Batch mode keeps the default behaviour: one LLM call sees all K candidates and produces one typed answer. Its case is built on three question types where sequential breaks down:  
批量模式保持默认行为：一次大语言模型调用查看全部K个候选并生成一个强类型答案。其适用性建立在顺序模式失效的三种问题类型上：  

Listing questions: “list all exclusions in this contract”. The answer is every matching candidate, not the first. Sequential would stop at top-1 (one exclusion found) and miss the other four. Batch is the only correct mode.  
列举问题：“列出本合同所有除外责任”。答案需包含所有匹配候选，而非第一个。顺序模式会在top-1停止（找到一个除外责任），遗漏其他四个。批量是唯一正确模式。  

Comparison questions: “is the premium higher than the previous year’s?”. The answer requires both candidates (this year + last year) in the same call so the LLM can compare them. Sequential would extract each one independently and lose the join.  
比较问题：“保费是否高于去年？”。答案需在同一调用中包含两个候选（今年+去年），以便大语言模型比较。顺序模式会独立提取每个候选，丢失关联性。  

Tight-score retrieval: when the top-K candidates’ relevance scores are within 5% of each other, retrieval cannot reliably promote top-1 to the top. Batch lets the LLM arbitrate with the full evidence visible.  
紧评分检索：当top-K候选的相关性分数彼此相差在5%以内时，检索无法可靠地将top-1置顶。批量模式让大语言模型在完整证据可见的情况下进行仲裁。  

Cost analysis: batch always pays the generation_cost(question + K chunks of context) once. Sequential pays generation_cost(question + 1 chunk of context) in the easy case (top-1 sufficient) and generation_cost(question + 1 chunk) × K in the worst case (every candidate insufficient).  
成本分析：批量始终支付一次 generation_cost(问题 + K个上下文片段)。顺序模式在简单情况（top-1足够）下支付 generation_cost(问题 + 1个片段)，在最坏情况（所有候选均不足）下支付 generation_cost(问题 + 1个片段) × K。  

On a typical enterprise corpus with K  
在典型企业语料中，K值通常为5-10。对于简单事实问题（约占70%），顺序模式节省60-80%成本；对于复杂问题（约占30%），批量模式更优。关键是根据问题类型智能分发。