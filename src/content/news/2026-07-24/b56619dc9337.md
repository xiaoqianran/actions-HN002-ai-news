---
title: "Most RAG Hallucinations Are Extraction Errors: Seven Patterns for a Typed Generation Contract"
originalUrl: "https://towardsdatascience.com/most-rag-hallucinations-are-extraction-errors-seven-patterns-for-a-typed-generation-contract/"
date: "2026-07-24T00:16:58.486Z"
---

**Title:** Most RAG Hallucinations Are Extraction Errors: Seven Patterns for a Typed Generation Contract  
**标题：** 大多数RAG幻觉实为提取错误：类型化生成契约的七种模式

---

Large Language Model Most RAG Hallucinations Are Extraction Errors: Seven Patterns for a Typed Generation Contract Enterprise Document Intelligence [Vol.1 #8ter] – Naming the RAG error correctly matters: model reads the context, so a wrong answer is an extraction error, not a hallucination. Seven typed-contract patterns keep the generation brick honest, with a decomposition rule for small models  
大型语言模型：大多数RAG幻觉实为提取错误——类型化生成契约的七种模式 | 企业文档智能 [第1卷 #8ter]——准确命名RAG错误至关重要：模型读取了上下文，因此错误答案是提取错误，而非幻觉。七种类型化契约模式确保生成模块的可靠性，并为小型模型提供分解规则

---

Kezhan Shi Jul 23, 2026 11 min read Share Photo by Kindel Media, via Pexels.  
石克战 | 2026年7月23日 | 11分钟阅读 | 分享 | 图片由Kindel Media通过Pexels提供

---

Not every question wants the same shape of answer. One wants a single number with a citation, another a list with one item per row, a third a yes or no with a caveat. Force them all through one generation call and each shape degrades the others. The fix is a small set of generation patterns, one per answer shape.  
并非所有问题都需要相同形式的答案。有的需要带引用的单个数字，有的需要每行一项的列表，有的则需要附带说明的是/否回答。若强制所有问题通过单一生成调用处理，每种形式都会降低其他形式的质量。解决方案是一组针对不同答案形式的生成模式。

---

This article is a companion to Enterprise Document Intelligence, the series whose philosophy is laid out in Amplify the Expert. It zooms in on brick 4 (generation) of the four-brick architecture, and pushes back on the vocabulary teams reach for when a RAG answer is wrong.  
本文是《企业文档智能》系列的 companion 文章，该系列的哲学在《放大专家》中阐述。本文聚焦于四层架构中的第4层（生成层），并反驳团队在RAG答案错误时常用的术语。

---

Hallucination, in the strict sense, is a fabrication from the model’s parametric memory: the LLM invents a fact without any grounding in the input. In RAG the model reads the context; when the answer is wrong, the cause is upstream, in the extraction chain (parsing, question, retrieval, or the generation contract itself). Calling every failure a hallucination closes the debate on where to act. Naming the real cause opens it back up.  
严格来说，幻觉是模型参数化记忆中的虚构：LLM在没有任何输入依据的情况下编造事实。在RAG中，模型会读取上下文；当答案错误时，原因在于上游的提取链（解析、问题、检索或生成契约本身）。将每次失败都称为幻觉会关闭关于行动位置的讨论。指出真正原因则会重新打开讨论。

---

The mainstream story has generation as send retrieved chunks + question to the LLM, get an answer string back. We reject this whole frame. The answer is not a string. It is a typed Pydantic object with citations, fidelity flags, and self-assessment fields. The LLM is a function, not an oracle. The schema is the contract. The validator runs before the user sees anything.  
主流叙事将生成定义为：将检索到的片段+问题发送给LLM，返回答案字符串。我们拒绝这一整个框架。答案不是字符串，而是带有引用、保真度标志和自我评估字段的类型化Pydantic对象。LLM是一个函数，而非神谕。模式即契约。验证器在用户看到任何内容之前运行。

---

where this article sits in the series: brick 8 (generation) highlighted – Image by author 📓 Reproduce the seven patterns in the shipped notebook: fill the typed AnswerWithEvidence schema on a broker-corpus question, watch each self-assessment flag flip, then rerun with the schema decomposed to see how a small model catches up to a frontier one. Repo → doc-intel/notebooks-vol1.  
本文在系列中的位置：突出显示第8层（生成）——作者图片 📓 在已发布的笔记本中重现七种模式：在经纪公司语料库问题上填充类型化AnswerWithEvidence模式，观察每个自我评估标志的变化，然后使用分解后的模式重新运行，观察小型模型如何追赶前沿模型。仓库 → doc-intel/notebooks-vol1

---

The public companion-code repo at doc-intel/notebooks-vol1 – Image by author The naive baseline this article pushes back on The architectural contrast: a free-text answer vs a typed contract the validator checks – Image by author  
公开的配套代码仓库位于 doc-intel/notebooks-vol1 —— 作者图片 本文反对的朴素基线 架构对比：自由文本答案 vs 验证器检查的类型化契约 —— 作者图片

---

The naive pipeline asks the LLM for an answer and trusts what comes back. There is no separation between found and invented, no place to record confidence, and no way to flag not found as a first-class outcome. We fill the schema instead: every field is a question the pipeline asks, and every answer is checkable.  
朴素管道向LLM请求答案并信任返回结果。其中没有区分“已找到”和“已发明”，没有记录置信度的位置，也没有将“未找到”作为一等结果标记的方法。我们转而填充模式：每个字段都是管道提出的问题，每个答案都可验证。

---

A string is one field. A typed contract is three regions, each with its own purpose and validator – Image by author Below are the seven patterns that keep generation on the typed-contract side. The first six are the contract itself. The seventh names a constraint the contract must respect when the model is small.  
字符串只是一个字段。类型化契约包含三个区域，每个区域都有其自身的目的和验证器——作者图片 以下是保持生成处于类型化契约一侧的七种模式。前六种是契约本身。第七种命名了契约在模型较小时必须遵守的约束。

---

**Pattern 1 – The LLM is a function, not an oracle**  
**模式1——LLM是函数，而非神谕**

The answer schema is a contract: structured retrieval in, a typed Pydantic object out, with citations, fidelity flags, and pipeline-feedback fields, never “the answer as a string”. Everything the generation brick does is in service of filling that contract, field by field.  
答案模式即契约：输入结构化检索结果，输出类型化Pydantic对象，包含引用、保真度标志和管道反馈字段，绝无“作为字符串的答案”。生成模块所做的一切都是为了逐字段填充该契约。

---

Take “what is the premium amount?”. The naive baseline returns a sentence: "The premium is $124 per month, per the contract.". To use that downstream you have to parse the number back out of the prose, and nothing tells you where it came from or how sure the model was. The series returns a row instead: Amount(value=124.0, currency="USD", unit="month", evidence=[Span(line_start=42, line_end=42, quote="premium of $124 / month")], answer_found=True, confidence=0.95). The value is already typed, the evidence points at the exact line, and the self-assessment fields travel with it. The caller reads structured values, not text it has to re-read.  
以“保费金额是多少？”为例。朴素基线返回一个句子：“根据合同，保费为每月124美元。”要在下游使用它，你必须从散文中重新解析数字，且没有任何信息告诉你数字来源或模型的置信度。本系列则返回一行数据：Amount(value=124.0, currency="USD", unit="month", evidence=[Span(line_start=42, line_end=42, quote="premium of $124 / month")], answer_found=True, confidence=0.95)。数值已是类型化的，证据指向确切行号，自我评估字段随之携带。调用方读取的是结构化值，而非需要重读的文本。

---

→ Article 8A (the answer contract) develops the typed contract in full.  
→ 第8A篇文章（答案契约）完整阐述了类型化契约。

---

**Pattern 2 – Extract typed values, never compute**  
**模式2——提取类型化值，永不计算**

Ask for Amount(value, currency, unit) and let Python compare with a visible exchange rate; computation inside the model erases the audit trail and silently does conversions no human can replay. The LLM extracts, Python computes.  
要求提供Amount(value, currency, unit)，让Python与可见汇率进行比较；模型内部的计算会擦除审计轨迹，并静默进行人类无法重现的转换。LLM负责提取，Python负责计算。

---

Take “is the premium higher than 100 euros per month?”. Ask the LLM directly and it answers "yes, the premium is around 130 EUR / month", having converted $124 to euros with a rate it never shows you. You cannot check the arithmetic, and next month the same question might convert at a different invisible rate. The series extracts the raw value instead, Amount(value=124, currency="USD", unit="month"), and lets Python do the comparison with a rate that sits in the audit log, 1 USD = 0.92 EUR. That gives 114 euros, so the answer is True, and every step, the extraction, the rate, and the comparison, can be replayed and checked by hand.  
以“保费是否高于每月100欧元？”为例。直接询问LLM，它会回答“是的，保费约为每月130欧元”，它将124美元转换为欧元时使用的汇率从未向你展示。你无法检查计算过程，且下个月同一问题可能使用不同的隐形汇率进行转换。本系列则提取原始值Amount(value=124, currency="USD", unit="month")，让Python使用审计日志中的汇率（1美元=0.92欧元）进行比较。结果为114欧元，因此答案为True，且每一步——提取、汇率、比较——都可以手动重放和检查。

---

→ Article 8A (the answer contract) covers typed values and the no-compute discipline.  
→ 第8A篇文章（答案契约）涵盖了类型化值和无计算准则。

---

**Pattern 3 – Completeness from structure, not self-rating**  
**模式3——完整性来自结构，而非自我评级**

The LLM inside the retrieval scope cannot see whether the next page continues a list; retrieval pulls one extra page and the pipeline checks for a section boundary, so completeness is deterministic and grounded in structure. A model that can only see what was sent to it cannot judge what was missing.  
检索范围内的LLM无法查看下一页是否延续列表；检索会多拉取一页，管道检查章节边界，因此完整性是确定性的且基于结构。只能看到所发送内容的模型无法判断缺失了什么。

---

Take “list all exclusions”. The exclusions section runs from page 7, and the next section, “Limitations”, starts on page 10, so the answer lives on pages 7 to 9. Naive RAG hands those pages to the LLM and asks “is the list complete?”. The model can only see what it was given, so it reads pages 7 to 9 and says yes, with no way to know whether a tenth exclusion was ever retrieved. The series checks the structure at retrieval time instead: does the next page still belong to the  
以“列出所有除外责任”为例。除外责任部分从第7页开始，下一节“限制”从第10页开始，因此答案位于第7至9页。朴素RAG将这些页面交给LLM并询问“列表是否完整？”。模型只能看到所给内容，因此它阅读第7至9页并回答“是”，但无法知道是否检索过第十条除外责任。本系列改为在检索时检查结构：下一页是否仍属于该部分？